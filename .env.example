# Environment Configuration

# Demo Server Port
DEMO_PORT=8787

# Zcash Mode: "mock" or "testnet"
# mock: Use generated mock proofs (faster, no external dependencies)
# testnet: Fetch real Zcash testnet data (requires internet connection)
ZCASH_MODE=mock

# Zcash RPC URL (only used when ZCASH_MODE=testnet)
ZCASH_RPC_URL=https://testnet.zcash.com

# Optional: RPC authentication (if using private node)
# ZCASH_RPC_USERNAME=
# ZCASH_RPC_PASSWORD=
